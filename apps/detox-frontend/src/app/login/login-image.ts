/**
 * A real photo from a real trip, not a stock library shot. Lives in its own
 * module because both the server page and the client hero import it, and a
 * client component may not import from a file that exports `metadata`.
 */
export const LOGIN_IMAGE =
  "https://pub-f5b50eb029e5430db1a9767ba1ee3421.r2.dev/packages/gallery/kodai-detox/kodai-detox-11.jpg";
