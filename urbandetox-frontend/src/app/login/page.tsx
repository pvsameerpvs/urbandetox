export default function LoginPage() {
  return (
    <div className="page-shell py-16 pb-24">
      <div className="spotlight-card max-w-2xl space-y-6">
        <p className="pill">Login preview</p>
        <h1 className="font-display text-5xl text-white">Auth moves in the Supabase phase.</h1>
        <p className="text-base leading-8 text-white/72">For now this prototype keeps browsing, booking, and admin review focused on the local SQLite-backed flow. The schema is already shaped to attach profiles to authenticated users later.</p>
      </div>
    </div>
  );
}
